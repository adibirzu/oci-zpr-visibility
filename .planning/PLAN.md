# OCI ZPR Visibility — Enhancement Plan

Generated by `/ecc:plan` on 2026-06-04. Companion to root [ROADMAP.md](../ROADMAP.md).

## Status at planning time

- Health: green — CI passing on origin, 34/34 tests, `git fsck` clean, no open GitHub issues, no TODO/FIXME.
- End-to-end validated in cap: 14/14 dashboard widgets HIT.
- Hygiene resolved this session: pushed `--version` + coverage-gate commits; committed `.claude/` (project-scoped ECC).
- No `.planning/` existed (created now). No GUI/webapp — only the native OCI Log Analytics dashboard.

## Decisions (from /ecc:plan)

1. **GUI direction: enhance the native OCI Log Analytics dashboard** (no new webapp). Add visualization metadata, KPI tiles, sunburst/flow-matrix/link-analysis widgets, severity color semantics, drill-downs, and a deploy script.
2. **Repo hygiene done immediately** (push + commit `.claude/`).

## Workstreams

### A. Dashboard / GUI attractiveness (primary)
The dashboard JSON has 14 bare widgets (`name` + `query` only) and is imported
manually. Make it attractive and one-command deployable. See
[dashboard-enhancement.md](dashboard-enhancement.md). Uses the ECC
`dashboard-builder` skill + the validated `OCI ZPR Visibility JSON` source.

### B. CLI hardening (ROADMAP P1 tail)
`--json` global flag + structured logging (replace `print`); typed run config;
centralized OCI retry/backoff + timeouts.

### C. Continuous operation (ROADMAP P2)
Scheduled collector (OCI Functions / OKE CronJob) + snapshot persistence for
drift; Monitoring alarms on finding severity & risky flow classifications.

### D. Coverage & correctness (ROADMAP P3)
Real VCN Flow Logs path (replace synthetic `trigger`); resource enrichment
beyond compute; finding-catalogue expansion.

## Sequencing

```
A (dashboard) ──┐
B (CLI hardening)├─ independent, parallelizable
C (scheduling)  │   C depends on B (config) for clean principals
D (coverage)    ┘   D depends on A's field/query contract staying stable
```

## Method

TDD per change (RED → GREEN → refactor), live e2e re-validated via
`oci-zpr-visibility validate-dashboards` (must stay 14/14+), CI + coverage gate
green before push. See [workflows.md](workflows.md).
