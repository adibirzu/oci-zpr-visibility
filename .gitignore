__pycache__/
*.py[cod]
.DS_Store
.pytest_cache/
.venv/
out/
*.log
*.egg-info/

.terraform/
*.tfstate
*.tfstate.*
terraform/terraform.tfvars
docs/dashboard-validate.json
.coverage
htmlcov/
orm/oci_zpr_visibility_pkg.tgz
# orm-stack.zip is committed on purpose: it is the target of the "Deploy to
# Oracle Cloud" button in README.md. Rebuild with scripts/build_orm_zip.sh.
orm/.terraform/
docs/evidence/*.log
!docs/evidence/*.redacted.log

# Long-form articles are published externally (Medium/Learn OCI), not tracked here.
# The diagrams they reference (docs/architecture/*) stay committed so the
# published article can link to them on GitHub.
docs/article-*.md

# Raw console captures (may contain OCIDs/IPs) — redact before committing
docs/evidence/screenshots/raw/

# Exported article PDFs (review artifacts, not committed)
docs/*.pdf
docs/_pdf_build_*.html
